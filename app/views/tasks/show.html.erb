<% if @task %>
  <p>
    <strong>Title:</strong>
    <%= @task.title %>
  </p>
  <p>
    <strong>Description:</strong>
    <%= @task.description %>
  </p>
  <!-- Add other task details here -->
<% else %>
  <p>Task not found.</p>
<% end %>
